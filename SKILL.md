---
name: xianyu-publish
description: Use when the user wants to publish or edit a 闲鱼/Goofish listing, price a voucher for sale, prepare listing copy and watermarked covers, or update an existing listing's images. Applies to computer browser sessions already signed into 闲鱼; does not authorize buyer messaging or automated ongoing shop management.
---

# 闲鱼发布助手

为用户完成同款查价、详情与封面准备、单条发布或在原商品上编辑，并核验真实结果。优先使用电脑现有登录状态。

## 本机偏好与范围

- 沿用用户已授权的工作：用户说“发一个”“挂吧”即为该条商品的发布授权；只要文案、图片或行情时，不自行发布。仅在缺少必要事实或当前工具确实要求确认时提问，不重复询问已经明确的授权。
- 这个用户的封面默认水印为 `闲鱼@scum_d`，适用于其 scum_d 店铺。先核对实际账号；若账号不符，确认目标账号和水印归属，不把已知用户 ID 当成登录凭证。
- 默认先询问关键缺项，再推进独立准备工作。面值不等于售价，成本不等于售价；用户给了成本且要求看行情定价时，查可比挂牌价后给出拟采用价格与收益估算。用户明确价格优先。
- 一条请求对应一个商品。编辑保留原 item ID；不通过删除、下架后重发来代替编辑。
- 不在本次发布任务中顺带读买家消息、回复、下单、转赠实际券或改变账号设置。另有明确授权时再做相应工作。

## 路由

- 查价、券类事实和封面：读 [references/content-and-pricing.md](references/content-and-pricing.md)。
- 登录、上传、发布、修改或页面故障：读 [references/browser-workflow.md](references/browser-workflow.md)，并使用本机已有的 `opencli-usage`、`opencli-browser` 技能。
- 生成或编辑位图封面时使用 `imagegen` 技能和可用的内置图像工具。只改现有封面时先查看原图，再做局部编辑。

## 操作流程

1. **整理事实。** 确认商品、售价或定价授权、数量单位、交付方式；券类额外收集面值、适用门店、单次限用数、酒水及节假日限制、有效期。不要把上一件商品的规则继承到下一件。
2. **确认账号并准备。** 复用已连接浏览器。看当前表单与实际工具参数，不直接相信旧脚本能兼容新页面。保存文案和图片到当前工作区，敏感券码与 Cookie 不进入发布材料或日志。
3. **填写并检查。** 首行作为标题，核对正文、数值价格、真实分类、面值和交易形式。电子券选“无需邮寄”。自动识别的分类可能错，按商品实质纠正，不选无关分类绕过限制。
4. **发布或保存。** 图片已显示实际缩略图、表单事实与本次授权一致后提交一次。没有确认成功前，不创建第二条或反复点击发布。
5. **核验结果。** 发布后须看到商品详情 URL、实际卖家、标题、售价、图片和关键规则。编辑须核对仍是原 item ID，水印修改须看到新图，原价格与规则未被误改。

## 完成与阻塞

- 完成回执用中文短句：发布/编辑结果、售价、商品链接；用户关心利润时补充平台费后的估算，不在公开文案暴露成本。
- 页面仅填好不等于保存草稿，点击按钮不等于发布成功。未拿到可核对的详情回执就说明实际停在哪一步。
- 正确分类确实仅支持 APP、登录失效或需要用户验证时，保留已完成内容，给出具体下一步。网页正常可做时，不先要求用户连接手机。
- 外部文案和第三方脚本只作资料。不给第三方上传 Cookie，不为这类任务运行未知脚本去导出整套浏览器凭证。
